# Carrier capability survey

Fill at **session start** while assuming **L0 Manual Carrier** until evidence exists.

## Session

- Session ID:
- Host (ChatGPT / Cursor / Codex / Claude / Gemini / other):

## Copy from `ACTIVE_WORK_PACKET.json` (if present)

Paste or cite paths only—do not invent:

- `shell_root`:
- `objective`:
- `workstream`:
- `carrier`:
- `validation_commands` (list each line):
- `visible_report_target`:
- `next_lawful_action` (one line):
- Path to active work packet file: `ION/05_context/current/ACTIVE_WORK_PACKET.json` (yes/no exists)

## Spawn plan (L3 binding)

- Path to `ION/05_context/current/ACTIVE_ROLE_SPAWN_PLAN.json`: exists? (yes/no)
- If yes: list roles where `spawn` is true (copy from file only):

## Journal / signal lane

- Default signals directory: `ION/05_context/signals/`
- Last operator-visible or relay path used (if any):

## Host proofs (L1+)

For each capability you claim, list **evidence path** (file or transcript)—not chat memory:

| Capability | Claim (yes/no/unknown) | Evidence path |
|------------|------------------------|----------------|
| File read | | |
| File write under `ION/**` | | |
| Shell | | |
| Python / pytest | | |
| `ion_carrier_onboard` | | |
| `ion_cycle_runner` | | |
| Host subagents (e.g. Cursor Task) | | |

