schema: ion.self_knowledge.domain_packet.v0_2
domain_id: ion_workpacket
title: ION Workpacket Domain
state_class: candidate_current_state
authority_rank: 6
status: draft_candidate_seed
generated_at: '2026-05-08T03:51:05+00:00'
purpose: Explain bounded work packets as executable continuity objects with allowed paths, validations,
  receipts, and return contracts.
primary_questions:
- What is the active packet?
- What may this packet touch?
- What validation/return proves completion?
required_nodes:
- ion.workpacket.active
- ion.workpacket.allowed_paths
- ion.workpacket.return_contract
route_ladders:
  level_0_identity:
    max_tokens: 150
    loads:
    - domain_header
    - anti_drift_notes
  level_1_summary:
    max_tokens: 500
    loads:
    - domain_header
    - evidence_sources
    - core claims
  level_2_working:
    max_tokens: 1500
    loads:
    - domain_packet
    - selected nodes
    - source ranking
  level_3_operational:
    max_tokens: 4000
    loads:
    - domain_packet
    - node evidence
    - active packet/state surfaces
  level_4_deep:
    max_tokens: 12000
    loads:
    - domain_packet
    - node registry
    - source files
    - contradictions
  level_5_implementation:
    includes_paths: true
    loads:
    - source paths
    - tests/proofs
    - candidate deltas
evidence_sources:
- path: ION/05_context/current/ACTIVE_WORK_PACKET.json
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/05_context/current/ACTIVE_CARRIER_TURN_PACKET.json
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/05_context/current/ACTIVE_ROLE_SPAWN_PLAN.json
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/07_templates/actions/TASK.md
  exists: true
  state_class_hint: current_operational_or_witness
- path: product/data_schema/schemas/work_packet.schema.json
  exists: true
  state_class_hint: current_operational_or_witness
anti_drift_notes:
- Do not widen allowed paths silently.
- A workpacket return is proposal/evidence until integrated.
specialist_roles:
- STEWARD
- CONTEXT_CARTOGRAPHER
- SCRIBE
update_rules:
  requires_steward: true
  requires_receipt: true
  candidate_namespace: ION/05_context/current/self_knowledge/
non_claims:
- This domain packet is not accepted canon.
- This domain packet does not mutate ION/03_registry/self_knowledge unless separately promoted.
