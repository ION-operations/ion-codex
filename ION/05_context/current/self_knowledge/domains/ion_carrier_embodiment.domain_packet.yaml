schema: ion.self_knowledge.domain_packet.v0_2
domain_id: ion_carrier_embodiment
title: ION Carrier Embodiment Domain
state_class: candidate_current_state
authority_rank: 6
status: draft_candidate_seed
generated_at: '2026-05-08T03:51:05+00:00'
purpose: 'Map host bodies to the same ION workflow: API, Codex CLI, ChatGPT sandbox, MCP/Gateway, browser,
  local workers.'
primary_questions:
- What body is ION running in?
- Which capabilities are available?
- What authority and side effects are allowed?
required_nodes:
- ion.carrier.gpt_sandbox
- ion.carrier.codex_cli
- ion.carrier.capability_profile
route_ladders:
  level_0_identity:
    max_tokens: 150
    loads:
    - domain_header
    - anti_drift_notes
  level_1_summary:
    max_tokens: 500
    loads:
    - domain_header
    - evidence_sources
    - core claims
  level_2_working:
    max_tokens: 1500
    loads:
    - domain_packet
    - selected nodes
    - source ranking
  level_3_operational:
    max_tokens: 4000
    loads:
    - domain_packet
    - node evidence
    - active packet/state surfaces
  level_4_deep:
    max_tokens: 12000
    loads:
    - domain_packet
    - node registry
    - source files
    - contradictions
  level_5_implementation:
    includes_paths: true
    loads:
    - source paths
    - tests/proofs
    - candidate deltas
evidence_sources:
- path: ION/03_registry/gpt_sandbox_carrier_profile.yaml
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/03_registry/codex_cli_carrier_profile.yaml
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/03_registry/carrier_capability_registry.yaml
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/02_architecture/ION_GPT_SANDBOX_ENVIRONMENT_CONTRACT.md
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/07_templates/carriers/GPT_SANDBOX_CARRIER_SESSION_PACKET.md
  exists: true
  state_class_hint: current_operational_or_witness
anti_drift_notes:
- Tool availability is not authority to mutate external state.
- GPT sandbox can edit/export the sandbox copy, not patch production/live repo.
specialist_roles:
- RUNTIME_CARTOGRAPHER
- STEWARD
update_rules:
  requires_steward: true
  requires_receipt: true
  candidate_namespace: ION/05_context/current/self_knowledge/
non_claims:
- This domain packet is not accepted canon.
- This domain packet does not mutate ION/03_registry/self_knowledge unless separately promoted.
