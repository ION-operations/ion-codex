# extension_agent_cockpit_modes

Required modes:
- Queue Registry
- Queue Detail
- Selected Request Start
- Approval Overlay
- Result Capture
- Settlement

Mode memory:
- Persist last mode per queue type.
- Restore on reload unless packet id invalidates selection.
