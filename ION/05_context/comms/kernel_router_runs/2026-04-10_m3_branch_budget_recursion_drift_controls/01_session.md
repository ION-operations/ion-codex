# M3 session

The working root now has one explicit branch-control layer.
Allocator law reads it before branch selection.
Settlement law reads it before fan-in classification.
Status can rediscover it through the latest receipt projection.
