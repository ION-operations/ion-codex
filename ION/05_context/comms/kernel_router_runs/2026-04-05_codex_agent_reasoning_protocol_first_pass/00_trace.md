# Kernel Router Trace — Codex Agent Reasoning Protocol First Pass

## Goal

Formalize the governed reasoning-window / anti-drift protocol in the repaired root without extending the contaminated witness-recursion lane.

## Steps

1. Read the repaired doctrine / architecture / template surfaces.
2. Land `AGENT_REASONING_PROTOCOL.md` as the current-phase operational protocol.
3. Land `T04_ReasoningWindowSchema.spec.md` to distinguish reasoning windows from context packages.
4. Restore `REASONING_JOURNAL` and `CODEX__REASONING_JOURNAL` into the active template floor.
5. Tighten `CODEX.boot.md`, `CODEX__CODE.md`, and Codex continuity so the protocol is actually active.
6. Run the full kernel suite to confirm the repaired root still holds.
