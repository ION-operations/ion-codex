# Root Authority Bundle — Start Here

This bundle is the read-tested stable carrier-facing startup surface for the current
workspace retained dual-center settlement.

Use this bundle before either `STATUS.md` when the question is:

- which ION root is primary right now,
- which one is still retained,
- where runtime history, signals, and current context live (`ION/05_context`).

## Current truthful posture

- **Packaged current-generation shell root**:
  `.`
  This is the package-aware shell root for editable install and pytest
  configuration.
- **Packaged current-generation root**:
  `ION`
  This is the primary content root and reintegration center inside the current
  settlement.
- **Top-level production root**:
  `../../ION`
  This remains the retained secondary extraction/promotion center for
  still-unpromoted MCP/API/docs surfaces.
- **Embedded residue lane**:
  `ION/05_context`
  This is runtime, signals, exports, and history under the packaged content
  root. A historical nested duplicate ION subtree was removed from this
  workspace; all lawful residue is under this lane.

## Startup rule

Start from the packaged current-generation content root for doctrine, code,
registry, and orchestration reading.

Start from the packaged current-generation shell root for package-aware shell
commands that depend on `pyproject.toml`, such as editable install and pytest
runs configured by that file.

Open the top-level production root only when the task is explicitly about
production-only surfaces still mapped in q003.

Do not treat either `STATUS.md` as the first startup authority for this
question. Read the bundle first. Single-root ratification is not authorized in
the current phase.

## Decision anchors

1. `ION/06_intelligence/decisions/2026-04-17_workspace_root_authority_canonicalization_decision.md`
2. `ION/06_intelligence/decisions/2026-04-17_top_level_production_surface_promotion_map_canonicalization_decision.md`
3. `ION/06_intelligence/decisions/2026-04-17_packaged_root_nested_path_disambiguation_canonicalization_decision.md`
4. `ION/06_intelligence/decisions/2026-04-17_retained_dual_center_settlement_canonicalization_decision.md`
5. `ION/06_intelligence/decisions/2026-04-17_external_transport_shell_current_phase_disposition_decision.md`

## Current forward-path anchor

If the question becomes "what now?" rather than only "which root?", read:

- `ION/06_intelligence/orchestration/2026-04-17_post_reintegration_canonicalization_state_forward_path_and_codex_handoff.md`

## What to do next

- Cursor/Codex users: read `CURSOR_CODEX_READ_MODE.md`
- browser ChatGPT users: read `BROWSER_CHATGPT_READ_MODE.md`
- Claude Code users: read `CLAUDE_CODE_READ_MODE.md`
