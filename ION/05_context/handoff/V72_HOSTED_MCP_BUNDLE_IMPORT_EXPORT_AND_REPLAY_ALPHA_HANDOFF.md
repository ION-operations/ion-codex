# V72 Handoff

V72 adds `kernel.ion_mcp_hosted_bundle_replay_alpha`, focused tests, schema, policy, and documentation for hosted bundle import/export/replay alpha. The branch accepts the V71 hosted storage ledger as substrate and defines deterministic bundle envelopes, import validation, preview replay plans, and tamper refusal.

Continue with V73 by surfacing bundle/replay evidence into the JOC cockpit or by adding a stronger persistent-file fixture for bundle round-trip tests. Do not add live execution.
