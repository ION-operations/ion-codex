# ION Starter Data

This is seeded portable continuity state for a first-time ION Custom GPT user or workspace.

It is not the engine. It is not source truth. It is not production authority. It is the continuity body that a compatible AI carrier may inspect, update by receipt, and export.

Use this with the main ION Custom GPT carrier package. The carrier package supplies law and behavior. This package supplies a blank state body so first-run work does not begin from an invented layout.

The user does not need to see these internals during first run. The AI carrier uses them to make continuity feel natural.

Default user-facing posture:

```text
ION first-time context is ready. What project, idea, codebase, document, or workflow should we start or attach?
```

Do not show file dumps unless the user asks for proof, diagnostics, or export details.
