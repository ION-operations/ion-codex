# Runtime worked examples

This layer exists to force the Lane C runtime/session review set through concrete runtime flows.

Purpose:
- show that `RUNTIME_SESSION_AUTHORITY_PROTOCOL`, `SESSION_QUEUE_AND_DISPATCH_PROTOCOL`, and `API_RUNTIME_ENTRY_PROTOCOL` remain distinct under enactment-like pressure
- demonstrate carrier/session/reporting behavior without smuggling Lane B activation law back into Lane C
- prepare the trio for future promotion-candidate review by replacing abstraction-only confidence with worked evidence

This layer remains review-space only.
Nothing here installs active architecture law.
