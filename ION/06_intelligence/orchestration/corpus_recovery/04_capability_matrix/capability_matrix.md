# Capability matrix

This first-pass matrix compares the four fractured cores against the main capability families.

Scale used:
- strong = strongest currently evidenced preservation center
- partial = meaningful but incomplete preservation
- weak = little or indirect preservation

## First-pass strongest-center judgments
- canonical workflow / packet law -> current branch
- current template binding / startup law -> current branch
- runtime / API / session / queue -> ION-BUILD
- manager / orchestrator / swarm -> Victus / Project-Gemini
- Aether constitution / kernel / atlas -> old Aether center, best materially preserved through ION-BUILD
- values precedence / smallest-law compression -> old Aether center

These judgments are provisional and must be tested against more detailed profile work and runnable proofs.


## Pass 4 executable precursor note
- Production `ION` and `ION (codex branch)` are now backed by full pytest receipts (`113 passed, 3 subtests passed` each).
- This strengthens the reading that Fractured Core A has both a current strongest line (current branch) and a runnable precursor pair inside the production estate.
- The `ION (codex branch)` line should still be read primarily as a documentation/state sibling rather than a distinct implementation family unless later contradictory evidence appears.


## Pass 5 executable note
- `IONv2` is now backed by a broad pytest receipt (`366 passed, 3 environment/plugin-bound async failures`).
- `conjugate-basis-hidden-field` is now backed by a small but real pytest receipt (`1 passed`).
- This strengthens the judgment that some smaller supporting lines are executable witnesses rather than purely archival residue.
- It also sharpens a key conflict: the current branch is not the only runnable line with modern-looking governance/continuity ambitions; `IONv2` remains a serious compact supporting implementation memory.


## Pass 6 note
- `operation-victus` remains the strongest executable activation-authority witness.
- `Project-Gemini` remains the strongest rehomed continuation witness of that same manager/orchestrator center.
- `IONv2` and the production precursor pair continue to preserve compact executable memory that the current branch does not fully compress anymore.


## SOS family executable pipeline witnesses

Pass 7 adds targeted pipeline receipts showing:
- `SOS` = `35 passed`
- `SOS-Gemini` = `35 passed`
- `SOS-OPUS` = `44 passed, 1 failed`

This matters because the SOS family should not be flattened into pure doctrine/governance siblings. At least part of that family remains compact and executable.
