# ION Self-Mount Completion Roadmap

**Status:** A3 branch roadmap  
**Date:** 2026-04-25  
**Branch:** `ION-GPT55-SELF-MOUNT`

## Goal

Complete the AI-facing continuity layer so future ChatGPT-class agents can mount
into ION with explicit self-definition, bounded authority, drift detection,
succession packets, and receipt-backed continuation.

## Phase V34 — Branch installation and consistency repair

```text
- install V34 delegation lock
- install GPT55 self-mount charter
- install agent self surface doctrine
- install continuity-of-self vs continuity-of-task doctrine
- install succession and drift protocols
- repair V33 production-readiness gap mismatch
- add kernel self-surface inspection surface
- add tests proving the branch remains non-production-authorized
```

## Phase V35 — Runtime identity envelopes

```text
- generate mounted_agent_identity envelopes at agent/session start
- expose identity envelope in front-door/operator receipts
- bind identity envelope to task packets and role chassis
```

## Phase V36 — Self-surface drift gate

```text
- implement S0-S5 drift classifier
- require halt/correction behavior for S3-S5
- add tests for false memory, false authority, and false production claims
```

## Phase V37 — Agent succession packets

```text
- define successor packet template
- generate succession receipts at meaningful closure points
- require successor read-before-act behavior
```

## Phase V38 — Self-mount graph integration

```text
- create context graph nodes for mounted agents
- link mounted agent identity to packets, roles, receipts, and changed files
- expose graph view of agent succession over time
```

## Phase V39 — Ratification review

```text
- run Steward/Vizier/Vice/Nemesis/Scribe/Vestige/Atlas review
- classify D5 self-definition claims
- promote only tested, reviewed, reversible surfaces
```

## Completion condition

The branch is complete only when a fresh agent can mount into the latest ION ZIP,
read its identity envelope, name its authority, identify its memory boundary,
continue the assigned task, detect self-drift, and leave a successor-ready
handoff without claiming false personal continuity.
