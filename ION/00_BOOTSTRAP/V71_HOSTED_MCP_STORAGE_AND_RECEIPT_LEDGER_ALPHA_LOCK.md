# V71 Hosted MCP Storage and Receipt Ledger Alpha Lock

This branch locks ION into hosted storage/receipt-ledger alpha mode.

V71 may define and preview hosted storage semantics. It may not certify hosted cloud production, Kubernetes, live execution, public endpoint readiness, provider dispatch, browser mutation, shell execution, credential access, or direct governed-write mutation.

All results must remain within:

- `READ_ONLY`
- `DRY_RUN`
- `APPROVAL_REQUIRED`
- `REFUSED`

`LIVE_EXECUTED` is forbidden.
