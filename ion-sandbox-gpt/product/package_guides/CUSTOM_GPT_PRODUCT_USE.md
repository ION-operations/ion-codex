# Custom GPT Product Use

The Custom GPT adapter material under `product/custom_gpt_adapter/` is support material for operating ION inside a browser AI sandbox.

It is not the whole product. The real ION surfaces live under `ION/`.

The intended product behavior is: the user experiences continuity, while the assistant operates with packets, context, templates, receipts, and state boundaries behind the scenes.
