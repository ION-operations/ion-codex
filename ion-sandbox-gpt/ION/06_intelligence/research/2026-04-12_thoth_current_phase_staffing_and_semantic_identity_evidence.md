---
type: research
authority: A3_OPERATIONAL
from: Thoth
created: 2026-04-12T17:10:00-04:00
status: COMPLETE
ratification: NOT_RATIFIED
topic: Active-branch evidence for current-phase staffing, semantic identity records, rank surfaces, and browser ChatGPT classification
connections:
  - ION/05_context/inbox/thoth_current_phase_staffing_and_semantic_identity_2026-04-12.task.md
  - ION/06_intelligence/research/2026-04-12_current_phase_staffing_and_semantic_identity_next_workload_plan.md
  - ION/06_intelligence/orchestration/2026-04-12_ion_acceptance_evidence_bundle_current_state.md
  - ION/06_intelligence/orchestration/2026-04-12_current_generation_ratification_record.md
  - ION/02_architecture/ROLE_CHASSIS_MOUNT_PROTOCOL.md
  - ION/02_architecture/TRUE_NAME_AND_SEMANTIC_LAYER_PROTOCOL.md
  - ION/02_architecture/RANK_AND_PRECEDENCE_PROTOCOL.md
  - ION/03_registry/current_phase_template_surface_registry.yaml
  - ION/03_registry/semantic_identities/README.md
  - ION/03_registry/boots/MASON.boot.md
  - ION/03_registry/boots/THOTH.boot.md
  - ION/03_registry/boots/VESTIGE.boot.md
---

# Thoth Evidence: Current-Phase Staffing And Semantic Identity

## Why this exists

Satisfy `ION/05_context/inbox/thoth_current_phase_staffing_and_semantic_identity_2026-04-12.task.md`:
extract what the **live branch files** currently say about semantic records, boot-plus-mount
carriage, rank/precedence, browser ChatGPT, and whether stronger closure is **already
required** or the branch still truthfully runs on **current-phase** posture.

## Sources or surfaces considered

Task load order plus acceptance bundle §9 for the ratified **open frontier** statement.

## Findings

### 1. Which roles have semantic identity records (`*.semantic.yaml`)

`ION/03_registry/semantic_identities/README.md` lists exactly four first-pass records:

```21:26:ION/03_registry/semantic_identities/README.md
## Current first-pass records

- `CODEX.semantic.yaml`
- `VIZIER.semantic.yaml`
- `VICE.semantic.yaml`
- `NEMESIS.semantic.yaml`
```

Directory listing on this branch shows only those four YAML files plus `README.md` (no
`MASON`, `THOTH`, `VESTIGE`, or browser semantic files).

**Observation:** Mason, Thoth, Vestige, and browser-class carriers have **no** dedicated
`*.semantic.yaml` in `ION/03_registry/semantic_identities/` on the active branch.

### 2. Which roles are carried by boots plus current-phase mount law only

`ROLE_CHASSIS_MOUNT_PROTOCOL.md` states that when no semantic identity record exists yet,
**boot plus protocol and packet assignment** may carry the mount:

```46:48:ION/02_architecture/ROLE_CHASSIS_MOUNT_PROTOCOL.md
4. If a stronger semantic or registry identity exists, use it.
5. If no semantic identity record exists yet for a live role, the active boot plus
   protocol and packet assignment may carry the current-phase mount.
```

The same protocol’s **non-goals** deny final staffing closure and full semantic records for
every live role:

```135:139:ION/02_architecture/ROLE_CHASSIS_MOUNT_PROTOCOL.md
## Non-goals

- no final constitutional staffing law
- no forced semantic activation of deeper names
- no claim that every live role already has a full semantic identity record
```

Boots for Mason, Thoth, and Vestige each declare **structural identity** and chassis
model in prose (examples):

```6:9:ION/03_registry/boots/MASON.boot.md
**Structural Identity:** Operative.Source.Software_Architect
**Tier:** 5 (bounded execution)
**Domain:** Source
**Model:** Composer 2 (near-unlimited usage — primary workhorse)
```

```6:9:ION/03_registry/boots/THOTH.boot.md
**Structural Identity:** Operative.Intelligence.Research_Analyst
**Tier:** 5 (bounded execution)
**Domain:** Intelligence
```

```7:11:ION/03_registry/boots/VESTIGE.boot.md
**Structural Identity:** Supervisor.Intelligence.Systems_Archaeologist
**Tier:** 4 (self-guided cross-file excavation; no release authority)
**Domain:** Intelligence
**Model:** Composer 2 (persistent, low-cost, read-heavy excavation chassis)
**Persistent:** true — you maintain continuity across sessions
```

`current_phase_template_surface_registry.yaml` records **Mason** and **Vestige** boots as
active mount anchors in `surface_records`; a search of that YAML file finds **no** entry
for `THOTH.boot.md` (Mason/Vestige appear; Thoth boot is absent from that registry on this
branch).

**Observation:** Support roles without `*.semantic.yaml` are still **explicitly lawful**
under current-phase law via boots and mount protocol; the branch **does not** claim every
role already has a semantic identity file.

### 3. Which rank / precedence surfaces apply directly

`RANK_AND_PRECEDENCE_PROTOCOL.md` gives the **primary precedence order** for conflict
interpretation (sovereign → doctrine → protocols → registry → …):

```43:55:ION/02_architecture/RANK_AND_PRECEDENCE_PROTOCOL.md
## 2. Primary precedence order

For the current migration, interpret conflicts in this order:

1. Sovereign / explicit human lead direction in the active session
2. Constitution and kernel doctrine in `ION/01_doctrine/`
3. Current active protocols in `ION/02_architecture/`
4. Registry state in `ION/03_registry/`
5. Active specs in `ION/06_intelligence/specs/`
6. Shared templates and bindings in `ION/07_templates/`
7. Runtime implementation in `ION/04_packages/`
8. Research, audits, signals, and other witness surfaces in `ION/05_context/` and `ION/06_intelligence/`
9. External lineage archives and production predecessor roots
```

It defines **first-pass rank classes** only for four entities:

```90:95:ION/02_architecture/RANK_AND_PRECEDENCE_PROTOCOL.md
Current first-pass mapping:

- `VIZIER` → `BURDEN_BEARER_ARCHITECTURAL`
- `CODEX` → `BOUNDED_CONSTRUCTION_LEAD`
- `VICE` → `INTERNAL_CONTRADICTION_PRESSURE`
- `NEMESIS` → `INDEPENDENT_AUDIT_GATE`
```

And it explicitly limits scope:

```97:107:ION/02_architecture/RANK_AND_PRECEDENCE_PROTOCOL.md
## 5. Scope of this protocol

This protocol governs:

- conflict interpretation,
- active packet review,
- semantic identity work,
- domain placement,
- and archive extraction discipline.

It does not yet define personnel law for every historical or future role.
```

**Observation:** Rank **classes** are directly mapped for Vizier, Codex, Vice, Nemesis
only; Mason / Thoth / Vestige / browser are **not** in that mapping table. The **precedence
stack** still applies globally when interpreting conflicts.

### 4. How browser ChatGPT is currently classified

`ROLE_CHASSIS_MOUNT_PROTOCOL.md` default interpretation:

```106:112:ION/02_architecture/ROLE_CHASSIS_MOUNT_PROTOCOL.md
### browser ChatGPT

browser ChatGPT remains `EXTERNAL_UNMOUNTED` by default in the current root until a
lawful mount packet plus stronger current-phase evidence says otherwise.

It may still operate as a bounded external carrier through the external-return bridge
surfaces without being silently promoted into an active named role.
```

**Observation:** Default classification is **`EXTERNAL_UNMOUNTED`**, with bounded
external carrier paths **without** silent promotion to an internal mounted role.

### 5. Ratified bundle: open frontier vs blocking

Post-ratification acceptance bundle lists **non-blocking** open items including staffing /
semantic identity for external chassis:

```235:243:ION/06_intelligence/orchestration/2026-04-12_ion_acceptance_evidence_bundle_current_state.md
### 9. Post-ratification frontier statement

Current-generation ratification does not erase all remaining frontier work.

The live branch still truthfully preserves these non-blocking open frontiers:

1. `ROLE_CHASSIS_MOUNT`, `DISAGREEMENT_ESCALATION`, and `EXTERNAL_RETURN` remain lawful current-phase bridge packets outside the canonical validator floor.
2. Final constitutional staffing law and final semantic identity law for every external chassis are still not globally settled.
```

**Observation:** The branch **names** unsettled final staffing/semantic law as **real** but
**not** a blocker to current-generation ratification.

### 6. Semantic layer protocol status

`TRUE_NAME_AND_SEMANTIC_LAYER_PROTOCOL.md` is **`ACTIVE_FIRST_PASS`**, not a total ontology:

```16:22:ION/02_architecture/TRUE_NAME_AND_SEMANTIC_LAYER_PROTOCOL.md
# TRUE NAME AND SEMANTIC LAYER PROTOCOL

## Status

This protocol formalizes the minimum semantic layer needed to stop casual rename drift in the live `ION/` root.
It is not yet a total ontology.
It is the first active naming law required for the current migration.
```

Registry rule for conflict with semantic files:

```126:133:ION/02_architecture/TRUE_NAME_AND_SEMANTIC_LAYER_PROTOCOL.md
## 6. Registry rule

The live semantic layer for current-phase governance is carried by:

- `ION/03_registry/semantic_identities/*.semantic.yaml`

When prose, task memory, or witness artifacts conflict with a semantic identity record,
the identity record governs for current-phase operational work unless a higher-authority doctrine surface explicitly overrides it.
```

**Observation:** Where a `*.semantic.yaml` exists (Codex, Vizier, Vice, Nemesis), it is the
governance anchor for those entities; other roles rely on boots and protocols until
records exist.

## Implications

- **Semantic files:** Four roles only; support field partially outside `*.semantic.yaml`.
- **Carriage:** Mount protocol **explicitly** allows boot-plus-packet carriage when no
  semantic file exists; non-goals **forbid** claiming full identity coverage.
- **Rank:** Precedence order is global; **rank class table** is narrow (four roles).
- **Browser:** **`EXTERNAL_UNMOUNTED`** by default.
- **Ratification:** Unsettled final staffing/semantic law is **acknowledged** as frontier,
  not as proof the generation was incomplete.

## Recommended next moves

Per `ION/06_intelligence/research/2026-04-12_current_phase_staffing_and_semantic_identity_next_workload_plan.md`,
Vestige archaeology plus Codex consolidation **after** evidence; Thoth does not pre-empt
that proposal here.

---

## Direct answer (task question)

**Does the active branch already require stronger semantic or staffing closure for the
support field, or is boot-plus-mount posture still the truthful current phase?**

The files above show **no mandatory “stronger closure now” requirement**: mount protocol
and non-goals **encode** boot-plus-mount as lawful when semantic records are absent;
rank protocol admits **incomplete** personnel law; acceptance bundle lists final staffing /
semantic identity for every external chassis as **unsettled but non-blocking**. Stronger
closure is a **candidate next workload** (per Codex plan and bundle §9), not something the
branch already states as a blocking defect of the current phase.

**Blocker:** None; conclusion is firm at the level of **file-visible** active-branch law.
