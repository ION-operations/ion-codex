---
type: orchestration_handoff
authority: A3_OPERATIONAL
created: 2026-04-10T15:44:00-04:00
status: ACTIVE
purpose: Consolidate the repo-grounded judgment that Codex can now work natively inside ION through manual/semi-automated canonical workflow carriage and define the next bounded path
---

# ION self-use alignment and forward path

## Current judgment

The active working branch already supports lawful Codex self-use.
The correct present mode is manual-first / semi-automated carriage of the canonical workflow using:

- private continuity
- bounded objectives
- reasoning-journal checkpoints
- route scaffolds
- role sessions
- handoffs
- and current operator CLI surfaces where they already exist

## What was directly verified

- `python -m kernel --help` exposes one real operator CLI with status, runtime, control, daemon, replay, child, external, packet, route, schedule, capability, equivalence, continuation, and allocator surfaces.
- `python -m kernel status` works and shows that the extracted root's store is effectively empty in this environment.
- `python -m kernel route research ...` can already render a lawful route scaffold for the current bounded objective.

## Interpretation

This means ION is already ready for native self-use under truthful constraints.
The current root should be used as:

- a lawful self-use environment,
- not yet a pretend always-on autonomous runtime,
- with manual fallback treated as the same process rather than a second process.

## Next bounded frontier

The next bounded frontier should be:

1. formalize the self-use execution protocol,
2. map constitutional domains to live modules,
3. declare the probabilistic/deterministic split explicitly,
4. define bootstrap/init law for fresh extracted roots,
5. then widen the daemon/runtime claim only where the code already justifies it.

## Handoff target

Fresh executor continuing from this packet should read:
- `ION/06_intelligence/research/2026-04-10_ion_kernel_constitution_self_use_alignment.md`
- `ION/06_intelligence/research/2026-04-10_ion_kernel_constitution_self_use_alignment_reasoning_journal.md`
- `ION/06_intelligence/orchestration/2026-04-10_ion_kernel_constitution_self_use_alignment_and_forward_path.md`
- `ION/05_context/comms/kernel_router_runs/2026-04-10_ion_kernel_constitution_self_use_alignment/`

## One-sentence directive

Keep building inside ION now, but do it through explicit packets, route scaffolds, reasoning journals, and handoffs until the deeper constitutional kernel and bootstrap law are fully declared.