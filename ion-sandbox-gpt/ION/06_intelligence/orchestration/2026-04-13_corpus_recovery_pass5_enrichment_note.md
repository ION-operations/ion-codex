# Corpus recovery pass 5 enrichment note

Pass 5 deepens three remaining Milestone 1 gaps:

1. it profiles additional supporting and wrapper lines that were still only implied,
2. it strengthens executable coverage with `IONv2` and `conjugate-basis-hidden-field` receipts,
3. it sharpens conflict capture around compact implementation memory and the historical meta-template center.

Most important outcomes:
- `IONv2` is now evidenced as a serious compact runnable line, not a minor archive footnote
- `conjugate-basis-hidden-field` is now evidenced as a small but real executable witness
- audit-wrapper roots are explicitly classified so they stop pretending to be independent centers
- the conflict between compact executable memory and heavier current-branch explicitness is now more explicit
