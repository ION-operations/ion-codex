# Corpus recovery Pass 24 — default project control kernel note

## What this pass adds

Pass 24 turns the stable operating kernel into an explicit **default project control kernel**.

Added:
- `00_program/project_control_kernel_manifest.csv`
- `00_program/default_project_control_kernel_judgment.md`
- `11_grand_picture/default_project_control_kernel.md`

## Why it matters

Earlier passes proved that the atlas had a stable operating kernel.
This pass makes the stronger judgment that the stable kernel is now good enough to act as the default long-lived work-start layer for the project.

That does not make the atlas final canon.
It makes the control start smaller, clearer, and harder to bypass.

## Main consequence

Future work should now default to the project control kernel first, widening into the broader atlas only when the task actually requires it.
