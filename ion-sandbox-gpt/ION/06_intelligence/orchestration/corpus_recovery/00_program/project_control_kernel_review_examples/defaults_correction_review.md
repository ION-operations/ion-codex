# Defaults correction review example

## Trigger
Repeated work-starts for the same question class return under the same insufficiency condition because the default center note is too weakly stated.

## Review path
- run kernel health checks
- result: strained but acceptable overall, patterned defaults mismatch detected
- repeated insufficiency attributable to defaults wording rather than to missing control primitives

## Governance outcome
- enter thaw for defaults correction

## Why
The kernel remains structurally sound, but the defaults layer is repeatedly routing future work into avoidable dead ends.
