# System equivalence map

## First-pass equivalence/similarity judgments
- `operation-victus` and `Project-Gemini` share a strong orchestrator/swarm family resemblance and likely preserve overlapping manager centers.
- current branch and production `ION`/`ION (codex branch)` belong to the same broad kernel family, but current branch is a later template-law/packet-law continuation.
- `ION-BUILD` is not simply another current-branch variant; it preserves a materially different center around runtime/API/session and Aether law.

These judgments remain provisional until deeper file-level comparison is complete.
