# 2026-04-13 production `ION` pytest receipt

## Source root
- `ION - Production/ION/`

## Command
```
cd "ION - Production/ION" && PYTHONPATH=04_packages python -m pytest -q
```

## Result
- `113 passed, 3 subtests passed`

## Interpretation
The production `ION` precursor line remains a real executable/tested witness, not only a doctrine or archive surface. It is smaller and earlier than the current branch, but it is strong enough to count as a runnable precursor center for Fractured Core A.
