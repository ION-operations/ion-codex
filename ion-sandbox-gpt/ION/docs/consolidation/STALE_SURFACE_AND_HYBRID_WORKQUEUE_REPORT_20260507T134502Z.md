# Stale Surface And Hybrid Workqueue Recovery Report

created_at: 2026-05-07T13:45:02+00:00
status: CANDIDATE_PACKAGE_LINEAGE_EXPORT_FOR_REVIEW
carrier: GPT_SANDBOX_CARRIER
production_authority: false
live_execution_authority: false

## Context Proof

Mounted package:

```text
ION_FULL_GPT_SANDBOX_AGENT_PACKAGE_v1_1_PERSONA_RELAY_SINGLE_CARRIER_20260507T124719Z.zip
```

Shell root:

```text
/mnt/data/ion_stale_surface_queue_work/ION_FULL_GPT_SANDBOX_AGENT_PACKAGE_v1_1
```

## Template Action Proof

template_id: `ion.template.stale_surface_audit_and_workqueue_patch.v1`
action_id: `stale_surface_hybrid_workqueue_20260507T134502Z`
result: `candidate_package_exported`

## Settlements

### Sandbox enforcement

The sandbox package can run structural/script enforcement when the package entry path invokes the gates. It is not a background daemon. This patch removes the known `run or manually apply` fallback and adds an audit to catch recurrence.

### Mini/Capsule

Mini/Capsule is classified as legacy/historical/witness/donor language only. The active replacement is bounded ContextPackage / compiled role context bundle / context-load proof / template-action proof / receipt-backed handoff.

### Cursor/subagents

Cursor subagents are adapter-scoped carrier slots, not the GPT sandbox baseline runtime and not ION roles. The GPT sandbox active packet now points to the single-carrier sequential packet.

### Hybrid product queue

The hybrid Custom GPT canvas was queued for v1.3 recovery without making raw development history default user state.

## Validation

```text
ion_status: ION_STATUS_READY
stale_surface_audit: ION_STALE_SURFACE_AUDIT_READY
single_carrier_sequence: ION_SINGLE_CARRIER_SEQUENCE_PACKET_READY
direct_validation: 11 passed / 11 total
```

## Non-claims

- No production authority.
- No live execution authority.
- No MCP use.
- No GitHub mutation.
- No external agent spawn.
- No historical archive promotion.
