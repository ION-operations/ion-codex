# Kernel router run — ION self-use alignment

This bundle records the bounded pass that verified native Codex self-use is already possible in the current ION working branch through the canonical workflow and live operator surfaces.
