schema: ion.self_knowledge.domain_packet.v0_2
domain_id: ion_architecture
title: ION Architecture Domain
state_class: candidate_current_state
authority_rank: 6
status: draft_candidate_seed
generated_at: '2026-05-08T03:51:05+00:00'
purpose: 'Explain structural ION: context graph, domains, templates, roles, packets, proof gates, receipts,
  settlement, route maps, runtime bodies.'
primary_questions:
- How is ION structured?
- How do domains/templates/context packages/receipts relate?
- Where does settlement occur?
required_nodes:
- ion.architecture.context_graph
- ion.architecture.templates
- ion.architecture.receipts
route_ladders:
  level_0_identity:
    max_tokens: 150
    loads:
    - domain_header
    - anti_drift_notes
  level_1_summary:
    max_tokens: 500
    loads:
    - domain_header
    - evidence_sources
    - core claims
  level_2_working:
    max_tokens: 1500
    loads:
    - domain_packet
    - selected nodes
    - source ranking
  level_3_operational:
    max_tokens: 4000
    loads:
    - domain_packet
    - node evidence
    - active packet/state surfaces
  level_4_deep:
    max_tokens: 12000
    loads:
    - domain_packet
    - node registry
    - source files
    - contradictions
  level_5_implementation:
    includes_paths: true
    loads:
    - source paths
    - tests/proofs
    - candidate deltas
evidence_sources:
- path: ION/02_architecture/ION_AGENT_CONTEXT_SYSTEMS_PROTOCOL.md
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/02_architecture/SELF_DOCUMENTING_CONTEXT_GRAPH_PROTOCOL.md
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/02_architecture/GRAPH_WRITEBACK_AND_RECEIPT_PROTOCOL.md
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/03_registry/context_graph_node_class_registry.yaml
  exists: true
  state_class_hint: current_operational_or_witness
- path: ION/07_templates/context/CONTEXT_PACKAGE.md
  exists: true
  state_class_hint: current_operational_or_witness
anti_drift_notes:
- A domain is a governed graph region, not a folder/topic bucket.
- Templates govern action shape; context packages govern the bounded working world.
specialist_roles:
- IONOLOGIST
- CONTEXT_CARTOGRAPHER
- TEMPLATE_CURATOR
update_rules:
  requires_steward: true
  requires_receipt: true
  candidate_namespace: ION/05_context/current/self_knowledge/
non_claims:
- This domain packet is not accepted canon.
- This domain packet does not mutate ION/03_registry/self_knowledge unless separately promoted.

