---
type: signal
from: Atlas
topic: systemd configuration extensions (systemd-confext) package
created: 2026-04-03
---

# ATLAS — systemd confext (`systemd-confext`)

**New package:** `ATLAS/systems/systemd-confext/` — full schema paths, `sources.yaml` (freedesktop **systemd-confext** + **systemd-confext.service** manuals), ledger **cfx-001**–**cfx-003**.

**Tag:** **`service-manager`** in `tag_index.yaml`; `systems_index` **primary_kind** **`service-manager`**.

**Relations:** **`integrates_with`** **`systemd`**, **`systemd-unit-model`**, **`systemd-sysext`**, **`systemd-portable`** (INFERRED), **`linux-kernel`**, **`unified-kernel-image`** (INFERRED); **`competes_with`** **`docker`**, **`oci-image-spec`** (INFERRED); reciprocals on **`systemd`**, **`systemd-sysext`**, **`systemd-unit-model`**, **`systemd-portable`**, **`linux-kernel`**, **`docker`**, **`oci-image-spec`**, **`unified-kernel-image`**.

**Comparative:** `ai_operating_system_reference_matrices.md` §8 + **Forbidden merges**; `context_systems_landscape.md`.

**Touch-up:** **`systemd`**, **`systemd-sysext`** identity/scope; **`systemd-sysext`** **`relations.json`**.

**Roadmap:** `_meta/AI_OS_EVOLUTION_ROADMAP.md` — confext wave.

**Witness:** root `ION/CAPSULE.md` **ION-099**; `ION/agents/atlas/CAPSULE.md` **A-059**.
