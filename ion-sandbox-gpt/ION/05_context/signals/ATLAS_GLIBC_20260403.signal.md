---
type: signal
from: Atlas
topic: GNU C Library (glibc) package
created: 2026-04-03
---

# ATLAS — GNU C Library (`glibc`)

**New package:** `ATLAS/systems/glibc/` — full schema paths, `sources.yaml` (GNU manual + sourceware), ledger **glibc-001**–**glibc-003**.

**Taxonomy:** **`c-runtime`** in `tag_taxonomy.yaml` and `tag_index.yaml`; `systems_index` **primary_kind** **`c-runtime`**.

**Relations:** **`integrates_with`** **`linux-kernel`**, **`gnu-gcc`**, **`gnu-binutils`**, **`elf`**, **`c-language`**, **`riscv-isa`**, **`clang`** (INFERRED), **`gnu-gdb`**, **`lldb`** (INFERRED); reciprocals on those packages.

**Comparative:** `language_machine_and_assembly_stack.md` §5; `ai_operating_system_reference_matrices.md` §9 + **Forbidden merges** + §10 gap; `context_systems_landscape.md` companion.

**Touch-up:** `gnu-gcc/01_scope.md` — **glibc** out-of-scope pointer.

**Roadmap:** `_meta/AI_OS_EVOLUTION_ROADMAP.md` — glibc wave; **Next waves** userland row.

**Witness:** root `ION/CAPSULE.md` **ION-093**; `ION/agents/atlas/CAPSULE.md` **A-053**.
